# BARBER — Landing Page Build Spec
**للتنفيذ المباشر عبر Claude Code — HTML + CSS + Vanilla JS، صفحة واحدة (Static)، RTL أولاً**

---

## 1. هوية العلامة

| العنصر | القيمة |
|---|---|
| اسم المحل | **BARBER** |
| التاجلاين الإنجليزي | "BARBER — KEEP IT SHARPER" |
| السب-لاين | "THE ORIGINAL BARBER" |
| جملة عربية اختيارية (تستخدم في قسم الفروع) | "اختاروا الأقرب" |
| اللوجو الأساسي | نسخة **الوش/الشنب** (Face mark): شعر بومبادور + شنب + دقن، فوقها/تحتها "BARBER" — الملف: `logo-face-mark.png` (مرفوع من المستخدم، خلفية شفافة) |
| لوجو ثانوي (اختياري لو احتجنا تنويع) | نسخة التاج (Crown) — لا تُستخدم كعلامة أساسية |

---

## 2. Design Tokens (مبنية على صور الديكور الفعلي: كونكريت + خشب + جلد أسود + نحاس)

```css
:root {
  /* خلفيات */
  --color-bg-primary: #141412;      /* أسود دافئ - خلفية الموقع الأساسية */
  --color-bg-secondary: #1e1e1c;    /* كروت/أقسام فرعية */
  --color-concrete: #7a7a73;        /* رمادي كونكريت - dividers, textures */
  --color-concrete-light: #b8b6ae;  /* كونكريت فاتح - نصوص ثانوية على خلفية غامقة */

  /* لمسات دافئة */
  --color-wood: #8a5c35;            /* خشب - accents، borders */
  --color-brass: #ab8253;           /* نحاس/برونز - بدل الدهبي اللامع، CTA وhighlights */
  --color-brass-light: #c9a877;     /* نحاس فاتح - hover states */

  /* نصوص */
  --color-text-primary: #f5f3ef;    /* أبيض دافئ */
  --color-text-muted: #a8a69f;

  /* أسود العناصر (كراسي، أزرار) */
  --color-black: #0a0a0a;
  --color-leather: #161613;

  /* خطوط */
  --font-heading-ar: 'Reem Kufi', sans-serif;
  --font-body-ar: 'Cairo', 'Tajawal', sans-serif;
  --font-heading-en: 'Playfair Display', serif;
  --font-body-en: 'Inter', sans-serif;
}
```

**ملاحظة تصميم مهمة**: القرار كان نتبع الـ vibe الفعلي للمحل (كونكريت+خشب+نحاس) مش نظام أسود+دهبي فاقع كان مقترح في البداية. الخلفية العامة تفضل غامقة (Bold Dark) لكن اللمسات اللونية (accents) تبقى نحاسية/خشبية مش دهبي لامع، وتتكرر صور الكونكريت/الخشب/الجلد الأسود كخلفيات حقيقية في الأقسام بدل الألوان الفلات بس.

---

## 3. بنية الصفحة (Sections بالترتيب)

### Header (Sticky)
- اللوجو (نسخة الوش/الشنب) يسار أو يمين حسب RTL
- روابط: الخدمات، الجاليري، الفروع، تواصل
- زرار واتساب صغير في الهيدر (أيقونة فقط على الموبايل)

### 1. Hero Section
- الخلفية: صورة حقيقية من صور المحل (الكراسي الجلد الأسود + كونكريت + خشب) — استخدم الصورة المرفوعة `interior-main.jpeg` مع overlay غامق للـ contrast
- اللوجو + "BARBER"
- التاجلاين: **"KEEP IT SHARPER"**
- زرارين:
  - **"شوف خدماتنا"** → scroll لقسم الخدمات (زرار أساسي، خلفية نحاسية)
  - **"كلمنا واتساب"** → يفتح واتساب على الرقم المركزي (زرار ثانوي، outline) — **ملحوظة: الاسم اتغير من "احجز الآن" لأنه مفيش نظام حجز**

### 2. الخدمات والأسعار
- Grid (3-4 أعمدة ديسكتوب / عمود واحد موبايل)
- كل كارت: أيقونة (Lucide/Feather، خطوط بس) + اسم الخدمة + وصف سطر واحد + السعر
- الخدمات (مبدئية - المستخدم يأكدها أو يعدلها):
  - قص شعر — `[PRICE]`
  - حلاقة دقن — `[PRICE]`
  - تشذيب دقن — `[PRICE]`
  - مساج — `[PRICE]`
  - صبغة — `[PRICE]`
  - شمع — `[PRICE]`
  - Combo (قص + دقن) — `[PRICE]`
- ⚠️ **الأسعار مش متوفرة لسه — حط placeholders واضحة `[PRICE]` جوه الكود عشان المستخدم يملاها**

### 3. الجاليري
- شبكة صور (Masonry أو Grid منتظم) لأعمال الحلاقين + صور المحل من الداخل
- Lightbox عند الضغط
- الصور المرفوعة المتاحة حالياً (تستخدم placeholder لحد ما تيجي صور أعمال فعلية):
  - `interior-1.jpeg` — صف الكراسي والمرايا
  - `interior-2.jpeg` — لوحة اللوجو على الحائط + الكنبة
  - صور أعمال القص/الحلاقة: **غير متوفرة لسه** — استخدم Unsplash placeholders بفلتر أبيض/أسود يتماشى مع الهوية

### 4. الفروع (5 كروت)

| الفرع | الموقع | تليفون | مواعيد |
|---|---|---|---|
| Craft Zone | بلوك 2 | `[PHONE]` | `[HOURS]` |
| B15 | بجوار محمد الفار ماركت | `[PHONE]` | `[HOURS]` |
| B12 | بجوار بيت الجملة ماركت | `[PHONE]` | `[HOURS]` |
| B10 | بجوار New Benny Market | `[PHONE]` | `[HOURS]` |
| B8 | خلف The Mart — 🌟 فرع جديد (Now Open) | `[PHONE]` | `[HOURS]` |

- كل كارت فرع: اسم + الموقع + زرار "افتح على Google Maps" (لينك خرائط لو متوفر، أو placeholder)
- فرع B8 يتحط عليه Badge "Now Open" زي ما ظاهر في التصميم المرجعي
- ⚠️ **العناوين التفصيلية، أرقام تليفونات كل فرع، ومواعيد العمل لسه ناقصة** — حالياً كل الفروع بتستخدم رقم الواتساب المركزي كـ fallback لحد ما توصل أرقام مستقلة

### 5. التواصل
- رقم الواتساب المركزي: **+20 11 59125229**
- إنستجرام: **[@barber._.eg](https://www.instagram.com/barber._.eg)**
- تيك توك: `[LINK]` — غير متوفر لسه
- أيقونات واضحة (Lucide) بدل فورم تواصل تقليدي، حسب البريف

### Footer
- حقوق النشر: "© 2026 BARBER — كل الحقوق محفوظة"
- روابط سوشيال (إنستجرام)
- خريطة مصغرة أو قائمة نصية بأسماء الفروع الخمسة

---

## 4. القيود التقنية (من البريف الأصلي — سارية)

- HTML5 + CSS3 (Grid/Flexbox) + Vanilla JS فقط، بدون فريموركات إلا لو استدعى الأمر
- Static بالكامل — بدون قاعدة بيانات أو باك إند
- RTL كامل للعربي، دعم اختياري للإنجليزي لاحقًا
- Mobile-First
- أداء: يفتح في أقل من ثانيتين
- Accessibility: alt text، contrast مناسب (لاحظ إن الخلفية غامقة فمحتاج تأكيد التباين مع النصوص الرمادية الفاتحة)
- SEO: meta tags + Open Graph + schema.org LocalBusiness (استخدم بيانات الفروع أعلاه في الـ schema)
- **لا يوجد نظام حجز أونلاين** — واتساب بس كوسيلة تواصل سريعة، مش CTA بمعنى "حجز"

---

## 6. Animations — أنيميشن الموقع ولوجو الـ Intro

### 6.1 أنيميشن عام بطابع حلاقة (Ambient / Micro-interactions)

1. **شعر متساقط خفيف في خلفية الـ Hero**
   - عناصر بسيطة (دوائر/خطوط رفيعة تمثل شعر) بعدد محدود (12-20 عنصر)
   - CSS `@keyframes fall` — تتحرك من أعلى لأسفل بسرعات وزوايا عشوائية بسيطة، وتتكرر (`infinite`)
   - شفافية منخفضة (`opacity: 0.08-0.15`) عشان متأثرش على وضوح النص فوقها
   - Pure CSS، بدون Canvas أو مكتبات — أداء خفيف

2. **Barber Pole كـ Section Divider**
   - شريط رفيع (30-40px ارتفاع) بين الأقسام، خطوط مائلة بألوان الهوية (أسود/نحاسي/كريمي) بدل الأحمر/الأزرق التقليدي
   - `background: repeating-linear-gradient(...)` مع `animation: barberpole-scroll` بتحرك `background-position` بشكل مستمر وبطيء
   - يتحط بين قسم الخدمات والجاليري مثلاً، كعنصر بصري مميز

3. **Micro-interaction على كروت الخدمات (Hover)**
   - عند الـ hover: خط رفيع بيتقطع/يتحرك زي خط مقص (clip-path animation أو border reveal) على حافة الكارت
   - يديله إحساس "قصّة نضيفة" بدل fade عادي

### 6.2 Logo Intro Animation — لوجو "BARBER → THE ORIGINAL BARBER"

**الأصول جاهزة بالفعل — مجلد `logo-layers/`:**

اللوجو الأصلي (`logo-full-transparent.png`، شفاف، 1024×1536) اتقسّم لـ 4 طبقات PNG شفافة منفصلة، كل واحدة بإحداثياتها الأصلية بالظبط داخل نفس الكانفاس (1024×1536) عشان تتجمع تاني بدقة 100%:

| الملف | x | y | width | height |
|---|---|---|---|---|
| `logo-hair.png` | 47 | 45 | 977 | 665 |
| `logo-text-barber.png` | 0 | 719 | 986 | 165 |
| `logo-mustache.png` | 15 | 931 | 947 | 188 |
| `logo-beard.png` | 109 | 1160 | 781 | 333 |

**طريقة الاستخدام:** container بـ `position: relative` بنسبة 1024:1536 (أو مصغّر بنفس الـ aspect ratio)، وكل صورة `position: absolute` بالـ `left/top` من الجدول (مقسومة على نسبة التصغير). كده الأربع طبقات بترجع تتجمع تظهر اللوجو الكامل بالظبط زي الأصل، وكل طبقة تتحرك لوحدها بـ `transform` بدون ما تأثر على مكان الباقي.

⚠️ دول **PNG شفافة عالية الدقة**، مش SVG حقيقي (الأصل مكنش متاح كملف فيكتور) — كافيين تمامًا للأنيميشن المطلوب. لو حبينا نكبّر اللوجو لمقاسات كبيرة جدًا من غير فقدان جودة مستقبلًا، السكة الوحيدة تحويله لـ Vector في برنامج تصميم.

**متطلبات إضافية:**
- الأنيميشن كـ **splash overlay** فوق الصفحة، ما يأخرش تحميل باقي المحتوى (المحتوى يحمّل في الخلفية بالتوازي)
- يشتغل **مرة واحدة فقط لكل session** — استخدام `sessionStorage.getItem('barber_intro_played')`؛ لو موجود، تخطي الأنيميشن وعرض اللوجو الثابت فورًا
- احترام `@media (prefers-reduced-motion: reduce)` — عرض الحالة النهائية فورًا بدون حركة
- إضافة زرار/منطقة "تخطي" خفية (tap/click في أي مكان) لتخطي الأنيميشن

**الـ Storyboard بالتفصيل:**

| # | التوقيت | العنصر | الحركة | تقنية مقترحة |
|---|---|---|---|---|
| 1 | 0.0–0.3s | نص "BARBER" | Fade-in أو Typewriter (حرف حرف) | `@keyframes fadeInLetters` مع `animation-delay` متدرج لكل حرف |
| 2 | 0.3–0.8s | شكل الشعر (Pompadour) | يسقط من أعلى الشاشة ("يخبط") ويستقر بارتداد بسيط | `transform: translateY()` من `-150%` إلى `0` مع `cubic-bezier(.36,1.56,.64,1)` (إيفيكت elastic/bounce) |
| 3 | 0.8–1.1s | الشنب | يزحلق أفقيًا من الجانب ويستقر تحت الشعر بـ overshoot خفيف | `transform: translateX()` مع نفس نوع الـ easing، أو keyframes متعددة الخطوات (0% → 110% → 100%) |
| 4 | 1.1–1.4s | اللوجو الكامل (الشعر+الشنب+النص) | ينزلق يسار الشاشة أو يصغر (scale down) لفسح مكان | `transform: translateX() scale()` بحركة سلسة `ease-out` |
| 5 | 1.4–2.0s | "THE ORIGINAL BARBER" | يظهر من خلف اللوجو (fade + slide بسيط) | `opacity` + `translateX` بدون bounce، حركة أهدأ من باقي المراحل |
| 6 | 2.0–2.3s | الأوفرلاي بالكامل | يختفي (fade out) ويكشف الـ Hero اللي خلفه | `opacity: 0` على الـ overlay div، ثم `display: none` أو `pointer-events: none` |

**ملاحظات تنفيذ إضافية:**
- تسلسل التوقيتات يتحقق بـ `animation-delay` على كل عنصر، مش بـ JavaScript timers، عشان الأداء يفضل سلس
- لو الحركة عايزة تتحكم أدق (staggering، sequencing معقد)، ممكن استخدام مكتبة خفيفة زي **anime.js (~17kb)** كاستثناء من قاعدة "بدون فريموركات" — لكن الأفضل نبدأ بـ CSS خالص الأول ونشوف لو كفاية

---

## 5. الفجوات المتبقية (لازم تتملى قبل/أثناء التنفيذ)

- [ ] أسعار الخدمات كاملة
- [ ] أرقام تليفونات مستقلة لكل فرع (لو موجودة، غير الرقم المركزي)
- [ ] العناوين التفصيلية لكل فرع (شارع/منطقة أدق من "بجوار...")
- [ ] مواعيد العمل لكل فرع
- [ ] لينكات Google Maps لكل فرع
- [ ] صور فعلية لأعمال الحلاقين (الجاليري حاليًا بصور المحل بس)
- [ ] لينك تيك توك (لو موجود)
- [x] ~~ملف اللوجو بصيغة SVG أو PNG عالي الدقة~~ — تم: `logo-layers/` (4 طبقات PNG شفافة + النسخة الكاملة)
